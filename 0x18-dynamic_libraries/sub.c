#include <stdio.h>

/**
 *_sub - subtraction of a & b.
 *@a: first number
 *@b: second number
 *Return: sub of a & b.
 */

int _sub(int a, int b)
{
    int subt;
    subt = a - b;
    return (subt);
}
