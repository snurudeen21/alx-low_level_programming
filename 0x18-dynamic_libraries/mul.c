#include <stdio.h>

/**
 *_mul - multiplication of a & b.
 *@a: first number
 *@b: second number
 *Return: mul of a & b.
 */

int _mul(int a, int b)
{
    int mult;
    mult = a + b;
    return (mult);
}
